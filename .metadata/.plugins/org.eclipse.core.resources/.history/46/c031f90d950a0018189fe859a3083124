
import static java.lang.System.*;

import java.awt.Graphics;

public class lab01d
{
   public static void main(String args[])
   {
	   Robot person = new Robot();
	   
	   person.paint(Graphics window );
	   person.head(Graphics window );
	   person.upperBody(Graphics window );
	   person.lowerBody(Graphics window ); 
      
      //call the methods needed to make the patterns on the word document
   }
}